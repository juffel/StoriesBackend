<h1>Geschichte anpassen</h1>
<div id="creation-date" data-creation-date="<%= @story.created_at %>">
    Hochgeladen <%= @story.created_at.strftime('um %H:%M am  %d.%m.%Y') %>
</div>
<div id="form">
    <form action="/stories/<%= @story.id %>/edit">
        <div>
            <input name="narrator" value="<%= @story.narrator %>">
                Name Erzähler
            </input>
        </div>
        <div>
            <input name="place" value="<%= @story.place %>">
                Ort des Geschehens
            </input>
        </div>
        <div>
            <input value="Ok!" type="submit"></input>
        </div>
    </form>
</div>
<%= audio_tag  %>

